[user]
	name = Jason Hamilton
	email = jwh@laydros.net
[core]
	excludesfile = ~/.gitignore_global
	editor = /usr/bin/vim
[color]
    ui = false
    interactive = auto
    diff = auto
    status = auto
    branch = auto
[color "interactive"]
    prompt = bold normal
    help = black
#[push]
#    default = simple
[status]
    relativePaths = false
[diff]
    external=/home/laydros/bin/git-meld.sh
[merge]
    external=/home/laydros/bin/git-meld.sh
    log = true
[alias]
    st = status -s -uno
    ix = diff --cached
    tree = log --oneline --decorate --graph
    news = log -p @{1}..
    lg = log --date=short --pretty='%cd %h%d %s'
    co = checkout
    d  = diff
    dc = diff --cached
    lc = log ORIG_HEAD.. --stat --no-merges
    gl = log --oneline --decorate --stat --graph
    st = status -sb
[push]
	default = simple
